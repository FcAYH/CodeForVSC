#include <bits/stdc++.h>

using namespace std;

void Solve()
{
    char st[2];
    scanf("%s", st);

    int t;
    scanf("%d", &t);

    while (t--)
    {
        int n, m;
        scanf("%d%d", &n, &m);

        // 什么b题，看不明白，不想做了
    }
}

int main()
{
    Solve();

    return 0;
}
