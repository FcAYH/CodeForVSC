#include <bits/stdc++.h>

using namespace std;

const int MAX_HEIGHT = 5;
const int MAX_WIDTH = 500;

char ans[MAX_HEIGHT][MAX_WIDTH];

void Solve()
{
    int n;
    scanf("%d", &n);

    char *str = new char[n + 1];
    scanf("%s", str);

    for (int i = 0; i < n; i++)
    {
        }
}

int main()
{
    Solve();

    return 0;
}
