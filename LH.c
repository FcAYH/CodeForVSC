#include <stdio.h>
#include <stdlib.h>

int main()
{
    int *p = (int *)malloc(sizeof(int) * 10);

    // for (size_t i = 0; i < count; i++)
    // {
    //     *(p + i) = i;
    // }

    // // const int *arr = p;
    // // arr[2] = 2;

    // free(root);
}
